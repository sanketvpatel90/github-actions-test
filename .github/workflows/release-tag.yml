name: release-tag

on:
  release:
    types: [published]

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Sleep for 6 sec
        run: sleep 6s
      - name: Echo a String
        run: echo "Released a new version (Tag)"
