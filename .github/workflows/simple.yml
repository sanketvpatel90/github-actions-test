name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a String
        run: eccho "Hello World!"
      - name: Multiline Scripts
        run: |
          node -v
          npm -v