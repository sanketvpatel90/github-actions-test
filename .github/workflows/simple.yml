name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a String
        run: echo "Hello World!"
      - name: Multiline Scripts
        run: |
          node -v
          npm -v